#!/usr/bin/make -f
# You must remove unused comment lines for the released package.
export DH_VERBOSE = 1

%:
	dh $@ -p pwdsphinx --with python3 --buildsystem=pybuild
	dh $@ -p pwdsphinx-tools --sourcedirectory=contrib

override_dh_auto_install:
	dh_auto_install
